# Claude Instructions

Read and follow `AGENTS.md` first. It is canonical.

If this file conflicts with `AGENTS.md`, `AGENTS.md` wins.

Treat `.claude/worktrees/` as local machine state. Do not rely on it or commit it.

## No AI attribution

Never add AI/Claude attribution to any committed or published artifact:

- No `Co-Authored-By: Claude ...` trailer in commit messages.
- No `🤖 Generated with [Claude Code](https://claude.com/claude-code)` footer (or any equivalent) in PR bodies.
- No "Created with Claude", "Generated by Claude", "AI-assisted" lines anywhere — commits, PRs, code comments, docs.
- No `claude` token in branch names. Use task-descriptive names following the existing convention (`feature/ts-XXX-...`, `chore/...`, `fix/...`, `refactor/...`).

Write commits, PR bodies, and branch names as if a human author created them. Don't ask whether to include the footer — the answer is always no.
